extends column2

block styles
 link(rel='stylesheet', href="reference.css")

block left-header
  h1.logo
    a(href="/") JUMLY

block right-header

block left-content
  .menu
    ul
      li Directives
        ul
          li
            a(href="#_found") @found
          li
            a(href="#_message") @message
          li
            a(href="#_create") @create
          li
            a(href="#_reply") @reply
          li
            a(href="#_alt") @alt
          li
            a(href="#_loop") @loop
          li
            a(href="#_ref") @ref
          li
            a(href="#_note") @note

block right-content
  h2#directives Directives

  
  #_found
    h3 @found
    p  is the top level element to start interactions.
     | It puts an <strong class="focusable" data-key='object' data-bind="event:{mouseover:focused,mouseout:unfocused}">new object</strong> with timeline and occurrence,
     | which are dashed line and gray-border rectangle.
    :jumly
      @found "He"
      he.attr "data-bind":"css:{foundobject:object()},event:{mouseover:focused,mouseout:unfocused}", "data-key":"object"
    :code(type=javascript)
      @found "He"
    
  h3#_message @message
  h3#_create @create
  h3#_reply @reply
  h3#_alt @alt
  h3#_loop @loop
  h3#_ref @ref
  h3#_note @note

  :coffeescript
    $ ->
      console.log "$ ->"
      $("body").on "ran.jumly", ->
        console.log "jumly.event", arguments
      f = ->
        foundmodel =
          aaa: ->console.log arguments
          focused: (vm, e)-> vm[$(e.target).data "key"] true
          unfocused: (vm, e)-> vm[$(e.target).data "key"] false
          object: ko.observable false
          timeline: ko.observable false
          occurrence: ko.observable false
        ko.applyBindings foundmodel, _found
        console.log "f = ->"
      #setTimeout f, 1000
      f()
