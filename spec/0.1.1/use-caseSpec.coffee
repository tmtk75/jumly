describe "use-case", ->

  it "should be rendered", ->
    script = """
             @actor ""
             @usecase "
             """
    diagramElement = $.jumly.build mkscript "use-case"

  describe "Builder", ->
